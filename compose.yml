version: '3'

services:
  # main:
  #   container_name: ichipro-api
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   ports:
  #     - 80:80

  db:
    image: postgres:16
    container_name: postgres_1
    ports:
      - 5432:5432
    volumes:
      - ./temp/store:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: $POSTGRES_USER
      POSTGRES_PASSWORD: $POSTGRES_PASSWORD
      POSTGRES_DB: $POSTGRES_DB
